% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crawler.R
\name{cr_parallel}
\alias{cr_parallel}
\title{Enable parallel (concurrent) fetching}
\usage{
cr_parallel(crawler, concurrency = 4L, max_active = NULL)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{concurrency}{Number of requests per batch.}

\item{max_active}{Maximum simultaneously-active connections (defaults to
\code{concurrency}).}
}
\value{
The crawler, invisibly.
}
\description{
Switches the HTTP engine to fetch requests in concurrent batches via
\code{httr2::req_perform_parallel()}, the rough equivalent of Crawlee's
autoscaled pool. Network I/O runs concurrently while handlers still run
sequentially in R, so there is no shared-state hazard. \code{robots.txt},
retries, \code{max_requests}/\code{max_depth} and queue checkpointing all still apply.
}
\details{
Parallel mode applies to the HTTP backend only; the browser backend always
runs sequentially. \code{delay} and \code{Crawl-delay} are applied \emph{between} batches.
}
\examples{
crawler("https://example.com") |> cr_parallel(concurrency = 8)
}
