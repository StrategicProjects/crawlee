% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persist.R
\name{cr_persist}
\alias{cr_persist}
\title{Persist a crawl to a run directory (and resume it)}
\usage{
cr_persist(crawler, dir, dataset = c("jsonl", "duckdb", "memory"))
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{dir}{Run directory (created if needed).}

\item{dataset}{Dataset backend to use: \code{"jsonl"} (default), \code{"duckdb"} or
\code{"memory"} (not persisted).}
}
\value{
The crawler, invisibly.
}
\description{
Wires a crawler to a directory on disk so a crawl is \strong{reproducible and
resumable}. It persists:
}
\details{
\itemize{
\item the request queue state (\code{queue.rds}) — pending requests, seen keys and
handled count, checkpointed during \code{\link[=cr_run]{cr_run()}};
\item the dataset, via a persistent \link{Dataset} backend (\code{dataset.jsonl} or
\code{dataset.duckdb});
\item binary content saved by \code{ctx$save_body()} (under \verb{kv/});
\item a run manifest (\code{manifest.rds}, plus \code{manifest.json} when \pkg{jsonlite}
is available).
}

If a queue state already exists in \code{dir}, the crawl \strong{resumes}: the saved
pending/seen/handled state is restored, so \code{\link[=cr_run]{cr_run()}} continues where it left
off and already-fetched URLs are not fetched again.

Call \code{\link[=cr_persist]{cr_persist()}} before \code{\link[=cr_run]{cr_run()}}. For the \code{"duckdb"} backend, collect
results with \code{\link[=cr_collect]{cr_collect()}} before \code{\link[=cr_close]{cr_close()}}.
}
\examples{
\dontrun{
crawler("https://example.com") |>
  cr_persist("runs/example", dataset = "duckdb") |>
  cr_on_html(\(ctx) ctx$push_data(list(url = ctx$request$url))) |>
  cr_run() |>
  cr_collect()
# Re-running the same pipeline resumes from runs/example.
}
}
