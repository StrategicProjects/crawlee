% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitemap.R
\name{cr_from_sitemap}
\alias{cr_from_sitemap}
\title{Discover URLs from a sitemap}
\usage{
cr_from_sitemap(
  crawler,
  url,
  label = NULL,
  include = NULL,
  exclude = NULL,
  since = NULL,
  max = Inf,
  max_levels = 3L
)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{url}{URL of a \code{sitemap.xml} or sitemap index.}

\item{label}{Optional handler label routing the enqueued URLs.}

\item{include, exclude}{Optional glob patterns (see \code{\link[=cr_on_html]{cr_on_html()}}).}

\item{since}{Optional date (or \code{YYYY-MM-DD} string); only URLs whose
\verb{<lastmod>} is on or after this date are enqueued (URLs without a
\code{lastmod} are kept).}

\item{max}{Maximum number of URLs to enqueue.}

\item{max_levels}{Maximum recursion depth into nested sitemap indexes.}
}
\value{
The crawler, invisibly.
}
\description{
Fetches a sitemap (or sitemap index, recursively) and enqueues the page URLs
it lists. Supports gzipped sitemaps, glob filtering and a \code{since} filter on
\verb{<lastmod>} for incremental re-crawls of large sites that publish dated
sitemaps.
}
\examples{
\dontrun{
crawler() |>
  cr_on_html(\(ctx) ctx$push_data(list(url = ctx$request$url))) |>
  cr_from_sitemap("https://example.com/sitemap.xml", since = "2026-01-01")
}
}
