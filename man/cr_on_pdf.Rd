% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf.R
\name{cr_on_pdf}
\alias{cr_on_pdf}
\title{Register a PDF handler}
\usage{
cr_on_pdf(crawler, handler, label = NULL)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{handler}{A function of one argument (the context). See \strong{Context}.}

\item{label}{Optional handler label; \code{NULL} registers the default PDF
handler.}
}
\value{
The crawler, invisibly.
}
\description{
Registers a handler invoked for responses classified as PDF — by
\code{Content-Type} (\code{application/pdf}) or a \code{.pdf} URL. The handler context adds
PDF-specific helpers on top of the usual ones.
}
\details{
Requests carrying an explicit \code{label} are always routed to the handler
registered for that label (regardless of content kind); \code{label = NULL}
registers the default PDF handler.
}
\section{Context}{

In addition to the elements documented in \code{\link[=cr_on_html]{cr_on_html()}}, a PDF handler's
context provides:
\describe{
\item{\code{kind}}{\code{"pdf"}.}
\item{\code{pdf_text()}}{Extract text per page (requires the \pkg{pdftools}
package), returning a character vector.}
\item{\code{body_raw()}}{The raw PDF bytes.}
\item{\code{save_body(key, ext)}}{Persist the PDF to the \link{KeyValueStore}.}
}
}

\examples{
\dontrun{
crawler("https://example.com/report.pdf") |>
  cr_on_pdf(function(ctx) {
    text <- ctx$pdf_text()
    ctx$push_data(list(url = ctx$request$url, n_pages = length(text)))
    ctx$save_body(ext = "pdf")
  }) |>
  cr_run()
}
}
