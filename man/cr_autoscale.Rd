% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crawler.R
\name{cr_autoscale}
\alias{cr_autoscale}
\title{Enable autoscaled parallel fetching}
\usage{
cr_autoscale(crawler, min = 1L, max = 16L, max_active = NULL)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{min, max}{Concurrency bounds. The crawl starts at \code{min}.}

\item{max_active}{Maximum simultaneously-active connections (defaults to the
current concurrency).}
}
\value{
The crawler, invisibly.
}
\description{
Like \code{\link[=cr_parallel]{cr_parallel()}}, but the batch concurrency adapts at run time, the
rough equivalent of Crawlee's autoscaled pool. After each batch the engine
adjusts concurrency with an additive-increase / multiplicative-decrease
rule: it grows by one when a batch is clean, and halves on back-pressure
(a transport failure or an HTTP 429/500/502/503/504), staying within
\verb{[min, max]}.
}
\examples{
crawler("https://example.com") |> cr_autoscale(min = 2, max = 16)
}
