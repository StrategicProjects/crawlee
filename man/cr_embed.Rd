% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rag.R
\name{cr_embed}
\alias{cr_embed}
\title{Attach embeddings to chunks}
\usage{
cr_embed(data, embed_fn, text_col = "text", batch_size = 32L)
}
\arguments{
\item{data}{A data frame with a text column (e.g. from \code{\link[=cr_chunk]{cr_chunk()}}).}

\item{embed_fn}{A function mapping a character vector to a numeric matrix
(rows = inputs) or a list of numeric vectors.}

\item{text_col}{Name of the text column. Defaults to \code{"text"}.}

\item{batch_size}{Number of texts per call to \code{embed_fn}.}
}
\value{
\code{data} with an added \code{embedding} list-column.
}
\description{
Adds an \code{embedding} list-column by applying a user-supplied, provider-
agnostic embedding function in batches. crawlee never calls an external
service itself: you pass \code{embed_fn}, which receives a character vector and
returns either a numeric matrix (one row per input) or a list of numeric
vectors. This keeps you free to use any provider or a local model.
}
\examples{
chunks <- cr_chunk(c("a b c d", "e f g h"), size = 2, overlap = 0, by = "word")
fake_embed <- function(x) matrix(nchar(x), nrow = length(x), ncol = 1)
cr_embed(chunks, fake_embed)
}
