% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handlers.R
\name{cr_on_html}
\alias{cr_on_html}
\title{Register an HTML handler}
\usage{
cr_on_html(crawler, handler, label = NULL)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{handler}{A function of one argument (the context). See \strong{Context}.}

\item{label}{Optional handler label. Requests enqueued with the same label
are routed here; \code{NULL} registers the default handler.}
}
\value{
The crawler, invisibly.
}
\description{
Registers a function called for each successfully fetched page whose request
carries the given \code{label} (or for all pages when \code{label = NULL}). The
handler receives a context object exposing the parsed page and the actions
\code{push_data()} and \code{enqueue_links()}.
}
\section{Context}{

The \code{ctx} passed to a handler contains:
\describe{
\item{\code{request}}{The request list (\code{url}, \code{label}, \code{depth}, ...).}
\item{\code{response}}{The \code{httr2} response object.}
\item{\code{page}}{The parsed page (an \code{xml_document}) or \code{NULL}.}
\item{\code{push_data(data)}}{Append a record to the dataset.}
\item{\code{enqueue_links(...)}}{Discover and enqueue links from the page.}
\item{\code{log}}{Logging functions (\code{info}, \code{success}, \code{warn}, \code{error}).}
}
}

\examples{
crawler("https://example.com") |>
  cr_on_html(function(ctx) {
    ctx$push_data(list(url = ctx$request$url))
    ctx$enqueue_links()
  })
}
