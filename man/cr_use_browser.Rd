% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/browser.R
\name{cr_use_browser}
\alias{cr_use_browser}
\title{Use the headless-browser fetch backend}
\usage{
cr_use_browser(crawler, wait = 0, wait_selector = NULL)
}
\arguments{
\item{crawler}{A \link{Crawler}.}

\item{wait}{Seconds to wait after page load before capturing the DOM
(useful for late-rendering content).}

\item{wait_selector}{Optional CSS selector to wait for before capturing.}
}
\value{
The crawler, invisibly.
}
\description{
Switches the crawler to render pages with a headless Chrome/Chromium via the
\pkg{chromote} package — for JavaScript-heavy sites where the plain HTTP
backend would see an empty shell. Handlers work exactly as with
\code{\link[=cr_use_http]{cr_use_http()}} (\code{ctx$page}, \code{enqueue_links()}, ...), and additionally gain
\code{ctx$screenshot()}.
}
\details{
Requires \pkg{chromote} and a Chrome/Chromium installation. PDF extraction
still requires the HTTP backend.
}
\examples{
\dontrun{
crawler("https://example.com") |>
  cr_use_browser(wait_selector = ".results") |>
  cr_on_html(\(ctx) {
    ctx$push_data(list(url = ctx$request$url))
    ctx$screenshot()
  }) |>
  cr_run()
}
}
