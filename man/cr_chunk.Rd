% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rag.R
\name{cr_chunk}
\alias{cr_chunk}
\title{Chunk text for retrieval-augmented generation}
\usage{
cr_chunk(
  data,
  text = NULL,
  size = 1000L,
  overlap = 200L,
  by = c("char", "word")
)
}
\arguments{
\item{data}{A character vector or a data frame (e.g. the result of
\code{\link[=cr_collect]{cr_collect()}}).}

\item{text}{When \code{data} is a data frame, the (unquoted) column holding the
text to chunk.}

\item{size}{Target chunk size, in characters (\code{by = "char"}) or words
(\code{by = "word"}).}

\item{overlap}{Overlap between consecutive chunks, in the same unit as
\code{size}. Must be smaller than \code{size}.}

\item{by}{\code{"char"} (default) or \code{"word"}.}
}
\value{
A tibble with columns \code{doc_id}, \code{chunk_id} (within document),
\code{chunk} (global index), \code{text}, \code{n_chars}, plus any carried metadata.
}
\description{
Splits documents into overlapping chunks suitable for embedding and
retrieval. Works on a character vector (one element per document) or on a
data frame, in which case the chunked column is replaced by \code{text} and all
other columns are carried along as per-chunk metadata.
}
\examples{
cr_chunk(c("um texto longo ...", "outro documento ..."),
         size = 10, overlap = 2, by = "word")
}
